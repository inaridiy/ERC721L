import { BigNumber } from "ethers";

const ONE = BigNumber.from(1);
const ZERO = BigNumber.from(0);
const BASE_PERIOD = BigNumber.from(7 * 24 * 60 * 60);
const MARGIN = BigNumber.from(10);

export { ONE, ZERO, BASE_PERIOD, MARGIN };
